# Use repo branch that includes latest syndicate auth fixes (e.g. challenges), or merge to your tracked branch.
[build]
builder = "NIXPACKS"
buildCommand = "npm ci && npm run build"

[deploy]
startCommand = "npm run start"
